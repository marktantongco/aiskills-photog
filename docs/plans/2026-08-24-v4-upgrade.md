# Plan — v4.0: Highlighting, Demo, Matrix, Schematic, Wiki, PDF

Skills used: find-skills (vercel-labs) · writing-plans · executing-plans · using-git-worktrees (adapted: platform pins session to `arena/01a02e0c-aiskills-photog`, so sequential sub-agents in one workspace) · verification-before-completion.

## Sub-agents
- A1 Researcher — select skills from skills.sh leaderboard (reputation + installs). DONE.
- A2 Architect — this plan. DONE.
- A3 UI Engineer — tasks 1–4.
- A4 Publishing Engineer — tasks 5–6.
- A5 QA — task 7 (jsdom evidence, RED→GREEN).
- A6 Release — task 8 (PR arena→master, merge, keep branch).

## Tasks (2–5 min each, TDD where testable)
1. **Search highlighting** — `script.js`: `clearHighlights()` + `highlightTextNodes()` (TreeWalker, text nodes only; never touches `data-copy` attrs); wire into `applyFilter`. CSS `mark.hl`.
2. **Schematic** — `index.html` #synergy: inline accessible SVG (title/desc, CSS-var colors) + `<details>` Mermaid `flowchart TD` source with copy chip.
3. **Matrix** — `index.html` new section #matrix: 6 domains × 4 levels, ✅/⚠️/❌ + **bold best**; nav + mobile links; `.matrix-row` participates in search filter.
4. **Demo** — `index.html` new section #demo: Prompt Scaffold Builder (6 selects → live assembled prompt, Randomize, Copy via existing copy system).
5. **Wiki** — `tools/build-wiki.py` parses `skills.md` → `wiki.html` (print-first, TOC, Print/Save-PDF button). Run, verify.
6. **PDF** — `tools/build-pdf.py` (fpdf2) parses `skills.md` → `skills.pdf` (sanitized Latin-1). Run, verify `%PDF` magic.
7. **QA** — extend jsdom suite: highlight add/clear, builder assembly+copy, matrix rows, SVG presence, wiki parses, PDF magic. ALL GREEN required.
8. **Release** — commit, push arena, `gh pr create` → master, `gh pr merge --merge` (no branch delete — session pinned).

## Verify gates
- G1: no runtime errors in jsdom boot.
- G2: search "rembrandt" → `<mark class="hl">` present in visible card; cleared on empty query.
- G3: builder change event → output pre text updates; Enter on output copies.
- G4: wiki.html + skills.pdf exist, serve 200, PDF starts with `%PDF`.
- G5: previous 22 tests still pass (no regressions).
